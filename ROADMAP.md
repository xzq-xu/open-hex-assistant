# Open Hex Assistant 路线图

语言：中文 | [English](ROADMAP.en.md)

本路线图是项目后续开发的优先级依据。新增功能、重构和发布计划都应先对齐这里的目标、边界和阶段，不符合路线图的功能默认延后或拒绝。

## 产品定位

Open Hex Assistant 要做的是免费开源的国服海克斯大乱斗助手：

- 国服数据优先。
- 本地优先，默认不上传截图或对局状态。
- 可解释推荐，而不是黑盒结论。
- 低延迟游戏内 overlay。
- 没有授权码、设备绑定、付费分层或闭源核心。

## 开发原则

- **结构化数据优先**：能从 LCU / Live Client Data / 公开统计拿到的数据，不用 OCR 或 AI 猜。
- **OCR 只补缺口**：OCR 用于识别游戏画面里没有稳定 API 的内容，例如三选一海克斯标题。
- **本地规则先行**：推荐引擎必须在无外部 API Key 时完整可用。
- **AI 可选增强**：外部 AI 只能作为用户显式开启的增强层，不能成为核心依赖。
- **隐私默认安全**：默认不上传截图、对局状态、召唤师信息或设备标识。
- **可解释输出**：每个推荐都要能说明数据依据、局势原因或样本风险。
- **不碰高风险能力**：不做读内存、注入、自动点击游戏按钮、绕过授权或闭源服务复刻。

## 版本路线

### v0.2：Windows 可用性基线

目标：让普通 Windows 用户能解压即用。

- 稳定 GitHub Release Windows zip。
- 打包内置 PaddleOCR ONNX 模型和字典。
- 完善首次启动、模型缺失、LCU 断开、OCR 异常的提示。
- 固化 `F6/F7/F8/F9` 热键行为。
- 验证全屏 / 无边框场景下 overlay 可见性。
- 补充 Windows 使用说明和常见问题。

### v0.3：推荐引擎升级

目标：从“排序”升级到“决策”。

- 引入已选海克斯历史。
- 把装备状态、敌方威胁、我方阵容纳入海克斯评分。
- 增加样本置信度和低样本风险提示。
- 为核心流派建立条件规则，例如暴击、技能急速、回复、前排、消耗、进场。
- 输出“推荐 / 次选 / 避免”的稳定解释格式。

### v0.4：数据缓存和版本管理

目标：让推荐数据可复现、可离线、可追踪。

- 本地缓存公开统计、DDragon、海克斯元数据。
- 记录 patch、数据更新时间、样本量和数据源。
- 支持数据过期提示和手动刷新。
- 增加 patch diff：哪些英雄、海克斯、装备变化明显。
- 提供离线模式，使用最后一次成功缓存。

### v0.5：Coach 完整化

目标：把局内 Coach 做成项目核心能力之一。

- 加载/开局自动分析双方阵容。
- 对局中通过 Live Client Data 持续维护装备、等级和威胁状态。
- `F9` 重评估从手动热键升级为“热键 + 状态变化触发”。
- Overlay 支持 Compact / Expanded 两种模式。
- 增加“下一件装备”“最大威胁”“团战策略”“海克斯方向”四个固定输出块。
- 为 TAB 记分板 OCR 做可选兜底，但结构化数据仍然优先。

### v0.6：可选 AI Provider

目标：只在本地规则足够扎实后，再提供 AI 增强。

- 支持 OpenAI Compatible / Gemini / Ollama 等用户自带 Key 的 provider。
- 默认关闭 AI。
- 只发送结构化摘要，不默认发送全屏截图。
- AI 输出必须是 JSON，由本地 renderer 渲染。
- UI 明确展示隐私提示、发送内容和关闭入口。
- AI 结果不得覆盖本地确定性推荐，只能补充解释或战术语言。

## 非目标

- 不做授权码或设备绑定。
- 不做强制联网。
- 不做闭源核心推荐逻辑。
- 不做自动游戏操作。
- 不做读内存、注入或绕过游戏保护。
- 不默认上传截图给第三方 AI。
- 不为了模仿竞品而牺牲本地可用性和可解释性。

## 近期任务池

- 把 Coach 规则拆成可测试的规则模块。
- 为海克斯推荐增加已选海克斯历史输入。
- 给数据加载层增加缓存和过期标记。
- 补充 Windows 实机 OCR / overlay 验证清单。
- 为 `runtime/overlay-state.example.json` 增加更多局势样例。
- 增加推荐引擎单元测试。

## 贡献判断

接受一个功能前，先回答四个问题：

1. 是否增强本地可用性、国服数据、推荐质量或透明度？
2. 是否默认保护用户隐私？
3. 是否能在无外部 AI / 无授权服务器时工作？
4. 是否有清晰的测试或验证方式？

如果答案大多是否定，这个功能不应该进入主线。
